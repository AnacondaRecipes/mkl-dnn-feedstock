anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: oneapi-src/oneDNN
      use_max_tag: true
      include_regex: ^v\d+\.\d+(?:\.\d+)?(?:-(?:rc|pc))?$
      from_pattern: ^v(.+)$
      to_pattern: \1
